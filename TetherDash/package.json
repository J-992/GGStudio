{
  "name": "tether-dash",
  "version": "0.1.0",
  "private": true,
  "description": "Endless tunnel runner where the walls and ceiling are floors too. Phaser 3, no dependencies: the build is a concatenation, not a bundle.",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "npx -y http-server . -p 8123 -c-1 -o",
    "start": "npx -y http-server . -p 8123 -c-1 -o",
    "check": "node tools/check.mjs",
    "build:poki": "node tools/build-poki.mjs",
    "package": "npm run check && npm run build:poki && node tools/package-poki.mjs",
    "preflight": "node ../scripts/poki/preflight.mjs ."
  }
}
